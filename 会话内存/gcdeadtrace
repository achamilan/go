gcdeadtrace 实现原理
====================

gcdeadtrace 是一个 GODEBUG 调试特性（GODEBUG=gcdeadtrace=1），在每次 GC 周期
结束时打印本周期内死亡对象的统计信息。

v2: 新增协程级会话追踪，GcDeadSessionStart()/End() 按 goroutine 隔离会话内分配。
v3: 新增会话存活对象追踪，区分"已释放"和"仍存活"的会话内分配。
v4: 新增 per-session 对象标记（span specials），支持多并发会话独立追踪。


1. 总体数据流

  用户分配: make([]byte, 256)
    → mallocgc → profilealloc → mProf_Malloc
        1. 采集调用栈 + 关联 bucket (原有)
        2. 若 goroutine 在会话中 → 更新 bucket 级会话计数 (v2)
        3. 若在会话中 → 在 span 上挂载 _KindSpecialGcDeadSession 特殊 (v4)

  GC sweep: 扫描 dead span
    → 遍历 span.specials
      → _KindSpecialProfile: mProf_Free (原有 profiling)
      → _KindSpecialGcDeadSession: gcDeadRecordFree (v4, per-session 释放)

  GC 结束: gcDeadTracePrint
    Phase 1: 遍历 mbuckets，收集 bucket 级会话计数 (v2/v3 兼容)
    Phase 2: 读取 gcDeadSessionTable → 输出 per-session 分解 (v4)
    Phase 3: 按分配站点排序并输出 freed/alive 报告


2. 核心数据结构

  ┌─────────────────────────────────────────────────────────────────┐
  │ bucket (保留原有 v2/v3 字段)                                     │
  │   hash, size, nstk, stack[]     // 分配调用栈 + 大小            │
  │   └→ memRecord {                                               │
  │        active   memRecordCycle   // 已发布的 profiling 快照     │
  │        future   [3]memRecordCycle // 三周期环形缓冲             │
  │                                                                    │
  │        // === gcdeadtrace 全局计数（保留，用于站点报告）=========   │
  │        gcDeadFrees     uintptr   // 本周期死亡对象数             │
  │        gcDeadFreeBytes uintptr   // 本周期死亡字节数             │
  │                                                                    │
  │        // === 会话追踪计数 (v2/v3) ==============================   │
  │        gcDeadSessionAllocs     uintptr // 会话分配次数(累计)     │
  │        gcDeadSessionAllocBytes uintptr // 会话分配字节(累计)     │
  │        gcDeadSessionFrees      uintptr // 会话释放数(每周期清零) │
  │        gcDeadSessionFreeBytes  uintptr // 会话释放字节(每周期清零)│
  │        gcDeadSessionCumFrees   uintptr // 会话释放数(累计)       │
  │        gcDeadSessionCumFreeBytes uintptr // 会话释放字节(累计)   │
  │      }                                                          │
  └─────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────┐
  │ specialGcDeadSession (mheap.go, v4 新增)                        │
  │   _    sys.NotInHeap                                            │
  │   special   special    // 嵌入 standard special 头部            │
  │   sessionID uint64     // 所属会话 ID                           │
  │   b         *bucket    // 分配时的 bucket 指针，用于站点归属     │
  │                                                                 │
  │   fixalloc: mheap_.specialGcDeadSessionAlloc                    │
  │   在 GC sweep 时通过 gcDeadRecordFree() 回调                    │
  └─────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────┐
  │ gcDeadSessionInfo (mprof.go)                                    │
  │   id           uint64  // 会话 ID (唯一)                         │
  │   goid         uint64  // 所属 goroutine ID                     │
  │   allocs       uintptr // 本周期分配数 (per-cycle)               │
  │   allocBytes   uintptr // 本周期分配字节 (per-cycle)             │
  │   frees        uintptr // 本周期释放数 (per-cycle)               │
  │   freeBytes    uintptr // 本周期释放字节 (per-cycle)             │
  │   cumAllocs    uintptr // 累计分配数                             │
  │   cumAllocBytes uintptr // 累计分配字节                          │
  │   cumFrees     uintptr // 累计释放数                             │
  │   cumFreeBytes uintptr // 累计释放字节                           │
  │                                                                 │
  │   // 每会话每桶释放计数，用于站点级 session 归属                   │
  │   bucketRefs [gcDeadPerSessionSites]gcDeadSessionBucketRef       │
  │                                                                 │
  │   var gcDeadSessionTable [gcDeadMaxSessions]gcDeadSessionInfo    │
  │   const gcDeadMaxSessions = 64                                   │
  │   const gcDeadPerSessionSites = 8                                │
  └─────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────┐
  │ gcDeadSessionBucketRef (mprof.go, 站点归属新增)                   │
  │   bucket unsafe.Pointer  // *bucket 指针，标识分配站点            │
  │   frees  uintptr         // 本周期该桶释放次数                    │
  │   bytes  uintptr         // 本周期该桶释放字节数                  │
  │                                                                 │
  │   gcDeadRecordFree 中通过 bucket 指针匹配，O(8) 线性探测          │
  │   每 GC 周期结束后清零 per-cycle 计数，保留 bucket 指针            │
  └─────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────┐
  │ g struct (runtime2.go)                                          │
  │   gcDeadSessionActive bool   // 当前 goroutine 是否在会话中     │
  │   gcDeadSessionID   uint64  // 当前 goroutine 绑定的会话 ID    │
  └─────────────────────────────────────────────────────────────────┘


3. 会话开启/关闭 — GcDeadSessionStart / GcDeadSessionEnd

  代码位置：mprof.go

  GcDeadSessionStart():
    1. debug.gcdeadtrace==0 → 直接返回
    2. 获取 getg().m.curg
    3. 若 gp==nil 或 gp.gcDeadSessionActive → 返回
    4. 从 gcDeadNextSessionID 分配唯一 sessionID
    5. 初始化 gcDeadSessionTable[idx] (id, goid)
    6. 设置 gp.gcDeadSessionID, gp.gcDeadSessionActive = true
    7. 自动管理采样周期（详见第 4 节）

  GcDeadSessionEnd():
    1-2. 同上
    3. 未激活 → 返回
    4. 清除 gp.gcDeadSessionActive = false, gp.gcDeadSessionID = 0
    5. 自动管理采样周期（详见第 4 节）


4. MemProfileRate 采样周期自动管理
====================================

  为什么需要管理采样周期？

    MemProfileRate 是 Go 内存 profiler 的采样率，默认为 512 KB。
    这意味着平均每分配 512 KB 才触发一次 mProf_Malloc。
    如果 gcdeadtrace 依赖 mProf_Malloc 来追踪会话分配，那么默认
    采样率下大部分小对象分配都不会被记录，导致 gcdeadtrace 报告
    严重不完整。

  自动管理机制：

    变量                             用途
    ─────────────────────────────────────────────────────────────
    gcDeadSessionCount  atomic.Int32  活跃会话计数
    gcDeadSavedRate     int           首个会话开始前的 MemProfileRate 值

  GcDeadSessionStart() 中:
    ┌──────────────────────────────────────────────────────────────┐
    │ if gcDeadSessionCount.Add(1) == 1 {   // 首个会话启动       │
    │     gcDeadSavedRate = MemProfileRate    // 保存原始值        │
    │     MemProfileRate = 1                  // 设置为全采集      │
    │ }                                                           │
    └──────────────────────────────────────────────────────────────┘

  GcDeadSessionEnd() 中:
    ┌──────────────────────────────────────────────────────────────┐
    │ if gcDeadSessionCount.Add(-1) == 0 {  // 末个会话结束       │
    │     MemProfileRate = gcDeadSavedRate   // 恢复原始值         │
    │ }                                                           │
    └──────────────────────────────────────────────────────────────┘

  设计要点：
    - 业务代码无需手动设置 MemProfileRate，框架自动管理
    - 首个会话开始时将采样率降为 1（每个分配都记录）
    - 所有会话结束后恢复原始采样率（避免影响正常 profiling）
    - gcDeadSessionCount 原子计数器支持嵌套会话（goroutine A 和
      goroutine B 同时开启会话，count=2，任一结束 count=1，
      最后一个结束才恢复）
    - 重复调用安全（幂等检查确保每个 goroutine 只计数一次）

  性能考量：
    - MemProfileRate=1 意味着每个 malloc 都调用 callers() + stkbucket()
    - 这对于调试期间的小型测试程序是可接受的
    - 生产环境不建议长时间开启 gcdeadtrace
    - 对应 GODEBUG 的临时调试特性定位

5. 分配时采集 — mProf_Malloc

  代码位置：mprof.go:461

  在 mProf_Malloc 中新增两条记录路径：

  路径 A — bucket 级会话标记 (v2/v3 兼容):
    ┌──────────────────────────────────────────────────────────────┐
    │ if gp.gcDeadSessionActive {                                   │
    │     atomic.Xadduintptr(&mr.gcDeadSessionAllocs, 1)            │
    │     atomic.Xadduintptr(&mr.gcDeadSessionAllocBytes, size)     │
    │ }                                                             │
    └──────────────────────────────────────────────────────────────┘

  路径 B — per-session 对象标记 (v4):
    ┌──────────────────────────────────────────────────────────────┐
    │ if gp.gcDeadSessionActive {                                   │
    │     // 更新 per-session 表                                    │
    │     e := &gcDeadSessionTable[sid % gcDeadMaxSessions]         │
    │     atomic.Xadduintptr(&e.allocs, 1)                          │
    │     atomic.Xadduintptr(&e.allocBytes, size)                   │
    │     atomic.Xadduintptr(&e.cumAllocs, 1)                       │
    │     atomic.Xadduintptr(&e.cumAllocBytes, size)                │
    │                                                               │
    │     // 在 span.specials 上挂载 _KindSpecialGcDeadSession      │
    │     ss := specialGcDeadSession{kind, sessionID}               │
    │     addspecial(p, &ss.special, false)                         │
    │ }                                                             │
    └──────────────────────────────────────────────────────────────┘

  关键区别：
    - 路径 A 在 bucket 上递增，用于按分配站点的 freed/alive 报告
    - 路径 B 在每个对象上挂载 special，当对象被 GC 释放时，
      freeSpecial 回调 gcDeadRecordFree(ss.sessionID, size)，
      精确地将释放归属于对应的会话


6. 释放时采集 — gcDeadRecordFree (v4)

  代码位置：mprof.go:657

  当 GC sweep 阶段遍历 dead span 的 specials 列表时，
  freeSpecial 函数遇到 _KindSpecialGcDeadSession 时调用：

    func gcDeadRecordFree(sessionID uint64, size uintptr, b *bucket) {
        idx := sessionID % gcDeadMaxSessions
        e := &gcDeadSessionTable[idx]
        if e.id != sessionID { return }  // 会话表项已被重用
        atomic.Xadduintptr(&e.frees, 1)
        atomic.Xadduintptr(&e.freeBytes, size)
        atomic.Xadduintptr(&e.cumFrees, 1)
        atomic.Xadduintptr(&e.cumFreeBytes, size)

        // 跟踪每(会话, 桶)释放数，用于站点级 session 归属
        if b != nil {
            for j := range e.bucketRefs {
                if e.bucketRefs[j].bucket == bp {
                    atomic.Xadduintptr(&e.bucketRefs[j].frees, 1)
                    atomic.Xadduintptr(&e.bucketRefs[j].bytes, size)
                    return
                }
            }
            // 新 bucket 插入空槽或 LRU 替换第一个
            ...
        }
    }

  设计要点：
    - 通过 sessionID % gcDeadMaxSessions 定位表项，O(1) 时间
    - 检查 e.id == sessionID 避免哈希冲突（旧会话被覆盖但特殊仍引用）
    - 同时更新 per-cycle (frees/freeBytes) 和 cumulative (cumFrees/cumFreeBytes)
    - 原子操作，无锁
    - bucket 指针和 session 表条目上的原子计数实现站点归属追踪

  精度保证：
    - 每个对象分配时挂载了所属会话的 sessionID，释放时精确回写
    - 不再依赖 bucket 级别的 AllocBytes > 0 启发式标记
    - 多会话共享同 bucket 时也能精确归属
    - 释放时同时记录 bucket 指针，使站点行能附加 [session #N: X objs, Y bytes]


7. GC 结束时打印 — gcDeadTracePrint

  代码位置：mprof.go:717

  七阶段流水线：

  Phase 1 — 收集 bucket 级原始数据（用于站点报告）
    遍历 mbuckets 链表，读取并清零 gcDeadFrees/gcDeadSessionFrees 等
    累积每个分配站点的 freed/alive 计数。合并相同 PC 元组的原始条目。
    重用已有 raw 条目时清空上一周期的 sessionRefs，防止陈旧数据残留。

  Phase 1.5 — 交叉引用 session bucketRefs 与 raw 条目（站点归属新增）
    遍历 gcDeadSessionTable，对每个活跃条目的 bucketRefs：
      - 提取 bucket 的调用栈 PC
      - 在 raw 条目中按 PC 元组匹配
      - 匹配成功 → 将 (sessionID, frees, bytes) 写入 raw[].sessionRefs

    gcDeadRawEntry 中存储 sessionRefs:
      sessionRefs     [4]struct{ sessionID uint64; frees uintptr; bytes uintptr }
      numSessionRefs int

    设计要点：
      - 每个 raw 条目最多挂载 4 个 session 归属（4 路并发安全余量）
      - 匹配基于原始 PC 元组（Phase 2 符号化之前），避免字符串比较
      - 同一桶跨会话释放时各自独立记录

    Phase 1.5 结束后清理 bucketRefs per-cycle 计数：
      将 gcDeadSessionTable 中所有 nonzero 的 bucketRefs.frees/bytes
      清零，防止下一周期从陈旧 bucket 指针产生虚假归属。

  Phase 2 — 读取 per-session 表（v4 新增）
    遍历 gcDeadSessionTable：
      - 对每个活跃条目 (id != 0)：
        - 读取 allocs, allocBytes (per-cycle)
        - 读取 frees, freeBytes (per-cycle)
        - 计算 alive = cumAllocs - cumFrees (累计)
          aliveBytes = cumAllocBytes - cumFreeBytes
        - 若任何计数 > 0 → 输出 per-session 分解行
        - 清零 per-cycle 计数器 (allocs/allocBytes/frees/freeBytes)
        - 保留累计计数器 (cumAllocs/cumAllocBytes/cumFrees/cumFreeBytes)

      输出格式：
        gcdeadsession by session:
          session #1 (gid=7): 4 allocs (1792 bytes), 3 freed (768 bytes), 1 alive (1024 bytes) [start: main.go:42, end: main.go:50]
          session #2 (gid=8): 3 allocs (1024 bytes), 2 freed (512 bytes), 1 alive (512 bytes)  [start: main.go:55]

      当 startPC 非 0 时在行末附加 [start: file:line]；endPC 非 0 时再附加 [, end: file:line]。
      第二个 session 未调用 GcDeadSessionEnd，因此无 end 标记。

  Phase 3 — 符号化（PC → 函数名/文件/行号）
    按分配站点维度合并并符号化，合并时合并 sessionRefs（去重 sessionID）

  Phase 4 — 排序
    按 bytes 降序

  Phase 5 — 格式化输出
    gcdeadsession:freed: N session objs (M bytes) freed from K sites
      funcName (file:line): N session objs, M session bytes [session #N: X objs, Y bytes]
      funcName (file:line): N session objs, M session bytes [session #N: X objs, Y bytes]
    gcdeadsession:alive: N session objs (M bytes) still alive from K sites
      funcName (file:line): N session objs, M session bytes

    gcdeadsession:freed 的每一行末尾附加 [session #N: X objs, Y bytes]，
    标明该周期内释放的内存属于哪个会话。alive 行不附加（存活对象不在
    当前周期的释放路径上，无法获取 bucket 指针回写）。

  Phase 6 — 写入输出
    write(2, buf, n) 到 stderr
    若 gcdeadtracefile 配置 → 追加写入文件


8. 站点级 session 归属原理

  gcdeadsession:freed 的站点行上附加 [session #N: X objs, Y bytes]
  是通过以下机制实现的：

  1. 分配时保存 bucket 指针到 special:
     mProf_Malloc 中，specialGcDeadSession.b = b (bucket 指针)

  2. 释放时通过 bucket 指针回写 session 表:
     gcDeadRecordFree(sessionID, size, b) 在会话表条目中查找
     或创建 gcDeadSessionBucketRef{bucket: bp, frees++, bytes+=size}

  3. GC 结束时交叉引用:
     gcDeadTracePrint Phase 1.5 中遍历会话表 bucketRefs，
     按 PC 元组匹配 raw 条目，写入 sessionRefs

  4. 输出时拼接:
     Phase 5 中遍历 site 的 sessionRefs，输出如 [session #3: 1 objs, 256 bytes]

  限制:
    - sessionRefs 数组大小为 4，超过时丢弃（8 路并发可调）
    - bucketRefs 数组大小为 8，超过时 LRU 替换第一个
    - GC 间清理机制确保不产生跨周期陈旧归属

9. 多会话隔离原理

  场景：两个并发 goroutine 同时启动会话，从相同分配站点 (make([]byte, 256))
  分配内存。传统 bucket 级追踪无法区分释放对象所属的会话。

  使用 per-session special (v4) 的隔离流程：

  分配时：
    Session A goroutine (gid=7) 分配 []byte{256}:
      → mProf_Malloc: 找到 bucket X (size=256, stack=...)
      → bucket.gcDeadSessionAllocs++ (用于站点报告)
      → gcDeadSessionTable[1].allocs++ (per-session)
      → addspecial: 挂载 special{sessionID: 1}

    Session B goroutine (gid=8) 分配 []byte{256}:
      → mProf_Malloc: 找到 SAME bucket X (相同 size+stack)
      → bucket.gcDeadSessionAllocs++ (站点报告会合并)
      → gcDeadSessionTable[2].allocs++ (per-session, 不同条目)
      → addspecial: 挂载 special{sessionID: 2}

  释放时（GC sweep）：
    Session A 的对象被释放:
      → freeSpecial → gcDeadRecordFree(sessionID=1, size=256)
      → gcDeadSessionTable[1].frees++ (只影响 Session A)

    Session B 的对象被释放:
      → freeSpecial → gcDeadRecordFree(sessionID=2, size=256)
      → gcDeadSessionTable[2].frees++ (只影响 Session B)

  GC 结束时输出:
    gcdeadsession by session:
      session #1 (gid=7): 4 allocs (1792 bytes), 3 freed (768 bytes), 1 alive (1024 bytes) [start: main.go:42, end: main.go:50]
      session #2 (gid=8): 3 allocs (1024 bytes), 2 freed (512 bytes), 1 alive (512 bytes)  [start: main.go:55]

    session #1 的三次释放仅包含其自身的释放，不混合 session #2。

  精度对比：
    - v2/v3 (bucket 级): 会话与非会话共享同一 bucket → 过计
    - v4 (per-session special): 每个对象带有 sessionID → 精确


10. 会话存活对象追踪 (v3/v4)

  通过累计计数追踪存活：

    分配: cumAllocs++ (仅更新)
    释放: cumFrees++  (同时更新 per-cycle 和 cumulative)
    GC:   alive = cumAllocs - cumFrees

  示例:
    ┌────────────────┬────────┬──────────┬──────────────────────────┐
    │ 会话           │ 大小   │ 状态     │ 每个 GC 周期的报告       │
    ├────────────────┼────────┼──────────┼──────────────────────────┤
    │ Session A gid7 │ 256B   │ 已释放   │ gcdeadsession:freed +    │
    │                │        │          │ session A frees++        │
    │ Session A gid7 │ 1024B  │ 仍存活   │ session A alive = 1      │
    │ Session B gid8 │ 256B   │ 已释放   │ gcdeadsession:freed +    │
    │                │        │          │ session B frees++        │
    │ Session B gid8 │ 512B   │ 仍存活   │ session B alive = 1      │
    └────────────────┴────────┴──────────┴──────────────────────────┘


11. per-session 表生命周期

  表条目 (`gcDeadSessionTable[sessionID % 64]`)：

  创建:
    GcDeadSessionStart() 分配 sessionID，初始化表条目
    id 和 goid 写入一次，永不改变

  活跃期:
    分配: 更新 allocs/allocBytes/cumAllocs/cumAllocBytes
    释放: (通过 specials 回调) 更新 frees/freeBytes/cumFrees/cumFreeBytes
          + 更新 bucketRefs 用于站点归属
    GC:   读取并清零 per-cycle 字段，保留累计字段

  重用:
    Session 结束后，表条目仍然存在（因为后续可能有 specials 释放回调）
    当 hash 冲突时，新 session 覆盖 id/init 字段
    gcDeadRecordFree 检查 e.id == sessionID → 旧 specials 的回调被安全忽略

  最大并发: 64 (gcDeadMaxSessions)
  Per-session 站点追踪上限: 8 (gcDeadPerSessionSites)，超过时 LRU 替换


12. 关键文件

  src/runtime/runtime2.go           — g.gcDeadSessionActive, g.gcDeadSessionID
  src/runtime/mheap.go              — specialGcDeadSession struct + fixalloc
  src/runtime/mprof.go              — gcDeadSessionTable + 所有追踪逻辑
  src/runtime/mprof.go:461          — mProf_Malloc special 挂载 + b 指针保存
  src/runtime/mprof.go:657          — gcDeadRecordFree (freeSpecial 回调 + bucketRefs)
  src/runtime/mprof.go:717          — gcDeadTracePrint (7 阶段流水线)
  src/runtime/mprof.go:1086         — GcDeadSessionStart (分配 sessionID)
  src/runtime/mprof.go:1115         — GcDeadSessionEnd (清除状态)
  src/runtime/mgc.go:1620           — GC 结束时调用 gcDeadTracePrint()
  src/runtime/runtime1.go:319       — debug.gcdeadtrace GODEBUG 注册
  src/runtime/create_file_unix.go   — Unix 文件写入 (O_APPEND)
  src/runtime/create_file_windows.go — Windows 文件写入 (FILE_APPEND_DATA)
  src/runtime/create_file_nounix.go  — 其他平台 (no-op)
  src/runtime/testdata/testprog/gc.go — 测试程序 (GCDeadTraceMultiSession 等)
  src/runtime/gc_test.go            — 测试函数 (TestGcDeadTraceMultiSession 等)


附录: GODEBUG 使用说明
======================

1. 基本用法

   // 启用 gcdeadtrace，输出到 stderr
   GODEBUG=gcdeadtrace=1 go run main.go

   // 同时输出到文件 (追加模式)
   GODEBUG=gcdeadtrace=1,gcdeadtracefile=gcdeadtrace.log go run main.go

   // 文件仅追加，不输出到 stderr
   GODEBUG=gcdeadtrace=1,gcdeadtracefile=gcdeadtrace.log go run main.go 2>/dev/null

2. 输出格式

   仅输出 session 数据，无全局 gcdead: 报告:

   === GC #1 ===
   gcdeadsession by session:
     session #1 (gid=7): 4 allocs (1792 bytes), 3 freed (768 bytes), 1 alive (1024 bytes) [start: main.go:42, end: main.go:50]
     session #2 (gid=8): 3 allocs (1024 bytes), 2 freed (512 bytes), 1 alive (512 bytes)  [start: main.go:55]

   gcdeadsession:freed: N session objs (M bytes) freed from K sites   ← 会话内分配且已释放
     funcName (file:line): N session objs, M session bytes [session #N: X objs, Y bytes]
     funcName (file:line): N session objs, M session bytes [session #N: X objs, Y bytes]
                                                      ↑ 每行末尾显示每会话归属

   gcdeadsession:alive: N session objs (M bytes) still alive from K sites  ← 会话内分配仍存活
     funcName (file:line): N session objs, M session bytes
     funcName (file:line): N session objs, M session bytes

3. Session API

   runtime.GcDeadSessionStart()
   // ... 分配会被追踪为 session 分配 ...
   runtime.GcDeadSessionEnd()
   // ... 后续分配不在 session 内 ...

4. 文件输出说明

   - gcdeadtracefile=<path> 追加模式，多次 GC 周期/多次运行的结果不丢失
   - 文件不存在自动创建
   - 与 stderr 输出同时生效（两者互不影响）
   - 只输出 gcdeadtrace 内容，不包含普通程序日志
