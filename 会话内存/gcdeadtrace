gcdeadtrace 实现原理
====================

gcdeadtrace 是一个 GODEBUG 调试特性（GODEBUG=gcdeadtrace=1），在每次 GC 周期
结束时打印本周期内死亡对象的统计信息。

v2: 新增协程级会话追踪，GcDeadSessionStart()/End() 按 goroutine 隔离会话内分配。
v3: 新增会话存活对象追踪，区分"已释放"和"仍存活"的会话内分配。


1. 总体数据流

  用户分配: make([]byte, 256)
    → mallocgc → profilealloc → mProf_Malloc   (采集调用栈 + 关联 bucket)

  GC sweep: 扫描 dead span
    → 读取 span.specials → mProf_Free           (累加死亡对象计数)

  GC 结束: gcDeadTracePrint                     (读取+清零计数 → 格式化输出)


2. 核心数据结构

  ┌─────────────────────────────────────────────────────────────────┐
  │ bucket                                                          │
  │   hash, size, nstk, stack[]     // 分配调用栈 + 大小            │
  │   └→ memRecord {                                               │
  │        active   memRecordCycle   // 已发布的 profiling 快照     │
  │        future   [3]memRecordCycle // 三周期环形缓冲 (C/C+1/C+2) │
  │                                                                    │
  │        // === gcdeadtrace 全局计数 (原子操作，GC 结束时清零) === │
  │        gcDeadFrees     uintptr   // 本周期死亡对象数            │
  │        gcDeadFreeBytes uintptr   // 本周期死亡字节数            │
  │                                                                    │
  │        // === 会话追踪计数 (v2) ===                               │
  │        gcDeadSessionAllocs     uintptr // 会话分配次数(累计)     │
  │        gcDeadSessionAllocBytes uintptr // 会话分配字节(累计)     │
  │        gcDeadSessionFrees      uintptr // 会话释放数(每周期清零) │
  │        gcDeadSessionFreeBytes  uintptr // 会话释放字节(每周期清零)│
  │        gcDeadSessionCumFrees     uintptr // 会话释放数(累计,v3) │
  │        gcDeadSessionCumFreeBytes uintptr // 会话释放字节(累计,v3)│
  │      }                                                          │
  └─────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────┐
  │ g struct (runtime2.go:552)                                      │
  │   gcDeadSessionActive bool  // 当前 goroutine 是否在会话中      │
  └─────────────────────────────────────────────────────────────────┘


3. 会话开启/关闭 — GcDeadSessionStart / GcDeadSessionEnd

  代码位置：mprof.go:849-873

  GcDeadSessionStart():
    1. debug.gcdeadtrace==0 → 直接返回（GODEBUG 未开启，零开销）
    2. 获取 getg().m.curg（当前用户 goroutine）
    3. 若 gp==nil 或 gp.gcDeadSessionActive==true → 返回（幂等）
    4. 设置 gp.gcDeadSessionActive = true

  GcDeadSessionEnd():
    1-2. 同上
    3. 若 gp==nil 或 gp.gcDeadSessionActive==false → 返回
    4. 清除 gp.gcDeadSessionActive = false

  设计要点：
    - 导出函数，用户代码通过 runtime.GcDeadSessionStart() 直接调用
    - 仅修改 g 结构体的 bool 字段，无锁，开销极小
    - 重复调用安全（幂等检查）


4. 分配时采集 — mProf_Malloc 会话扩展

  代码位置：mprof.go:452-488

  原有流程：
    1. callers(3, ...) 捕获调用栈
    2. stkbucket(...) 查找/创建 bucket
    3. lock → allocs++ → unlock
    4. setprofilebucket 关联 bucket 到 span

  会话扩展（步骤 3 之后，步骤 4 之前）：
    ┌──────────────────────────────────────────────────────────────┐
    │ if debug.gcdeadtrace > 0 {                                   │
    │     gp := mp.curg              // 获取当前用户 goroutine     │
    │     if gp != nil && gp.gcDeadSessionActive {                 │
    │         atomic.Xadduintptr(&mr.gcDeadSessionAllocs, 1)       │  ← v3
    │         atomic.Xadduintptr(&mr.gcDeadSessionAllocBytes, size)│
    │     }                                                        │
    │ }                                                            │
    └──────────────────────────────────────────────────────────────┘

  核心思想：
    - 此时 mProf_Malloc 持有 mp（当前 M），通过 mp.curg 拿到当前用户 G
    - 检查 g.gcDeadSessionActive：若 true → 在 bucket 上原子递增：
      · gcDeadSessionAllocs（累计分配次数，v3 新增）
      · gcDeadSessionAllocBytes（累计分配字节，标记 + 计数）
    - 两个字段都是累计值，永不归零

  为什么在 lock 之外做？
    - 原子操作不需要锁保护
    - setprofilebucket 内部有锁，避免锁嵌套


5. 释放时采集 — mProf_Free 会话扩展

  代码位置：mprof.go:491-509

  原有流程：
    1. index := (mProfCycle + 1) % 3  // free 计入 C+1 周期
    2. lock → frees++ → unlock

  gcdeadtrace 部分（在 lock 内部）：
    ┌──────────────────────────────────────────────────────────────┐
    │ if debug.gcdeadtrace > 0 {                                   │
    │     atomic.Xadduintptr(&mp.gcDeadFrees, 1)                   │
    │     atomic.Xadduintptr(&mp.gcDeadFreeBytes, size)            │
    │                                                              │
    │     if mp.gcDeadSessionAllocBytes > 0 {                      │
    │         atomic.Xadduintptr(&mp.gcDeadSessionFrees, 1)        │
    │         atomic.Xadduintptr(&mp.gcDeadSessionFreeBytes, size) │
    │         atomic.Xadduintptr(&mp.gcDeadSessionCumFrees, 1)     │  ← v3
    │         atomic.Xadduintptr(&mp.gcDeadSessionCumFreeBytes, size)│← v3
    │     }                                                        │
    │ }                                                            │
    └──────────────────────────────────────────────────────────────┘

  四个会话计数器的角色：
    gcDeadSessionFrees / FreeBytes    → 每周期清零，用于"本周期释放"报告
    gcDeadSessionCumFrees / CumFreeBytes → 累计值，用于计算"仍存活"

  仍存活 = 累计分配 - 累计释放
    sessionAlive = gcDeadSessionAllocs - gcDeadSessionCumFrees
    sessionAliveBytes = gcDeadSessionAllocBytes - gcDeadSessionCumFreeBytes

  精度权衡：
    ✓ 若 bucket 专属于会话 goroutine → 精确
    ✗ 若 bucket 被会话和非会话 goroutine 共享 → 会过计（非会话释放
      也被归因为会话释放）
    → 对专用 worker goroutine（固定分配大小）是精确的；v1 调试特性
      精度可接受


6. GC 结束时打印 — gcDeadTracePrint

  代码位置：mprof.go:557-843

  五阶段流水线：

  Phase 1 — 收集原始数据（profMemActiveLock 内，无符号查找）
    ┌──────────────────────────────────────────────────────────────┐
    │ for b := mbuckets 链表 {                                     │
    │     f  = Xchguintptr(&mp.gcDeadFrees, 0)       // 读+清零    │
    │     b2 = Xchguintptr(&mp.gcDeadFreeBytes, 0)                 │
    │     sf = Xchguintptr(&mp.gcDeadSessionFrees, 0)              │
    │     sb = Xchguintptr(&mp.gcDeadSessionFreeBytes, 0)          │
    │                                                              │
    │     sa   = Load(&mp.gcDeadSessionAllocs)        // 累计(不归零)│
    │     sab  = Load(&mp.gcDeadSessionAllocBytes)                 │
    │     scf  = Load(&mp.gcDeadSessionCumFrees)      // 累计(不归零)│
    │     scfb = Load(&mp.gcDeadSessionCumFreeBytes)               │
    │                                                              │
    │     // 仍存活 = 累计分配 - 累计释放                          │
    │     alive = sessAllocs - sessCumFrees (若>0)                 │
    │     aliveBytes = sessAllocBytes - sessCumFreeBytes           │
    │                                                              │
    │     if f == 0 && sf == 0 && alive == 0 { continue }          │
    │                                                              │
    │     遍历 stack，跳过 "runtime." 前缀帧                       │
    │     按 PC tuple 合并到 raw[]（含 allocs/frees/cumulative/alive）│
    │ }                                                            │
    └──────────────────────────────────────────────────────────────┘

  Phase 2 — 符号化（PC → 函数名/文件/行号）
    将 raw[] 中的 PC 转换为符号信息，按 (funcs, file, line) 合并到
    sites[]，同步累加所有会话字段

  Phase 3 — 排序
    按 bytes 降序排列 sites[]

  Phase 4 — 格式化输出 (v3)
    ┌──────────────────────────────────────────────────────────────┐
    │ gcdeadsession:freed: N session objs (M bytes) freed from ... │
    │   funcName (file:line): N session objs, M session bytes      │
    │                                                              │
    │ gcdeadsession:alive: N session objs (M bytes) still alive .. │
    │   funcName (file:line): N session objs, M session bytes      │
    └──────────────────────────────────────────────────────────────┘

    全局 gcdead: 段落已移除，只输出 session 数据。这样可以避免非
    session 协程的分配噪声，只关注会话内的内存使用。

    gcdeadsession:freed: 本周期内会话分配 → 已被 GC 释放的对象
      (数据来源: gcDeadSessionFrees/FreeBytes，每周期清零)
    gcdeadsession:alive: 会话分配 → 至今未被释放的对象
      (数据来源: Allocs - CumFrees, AllocBytes - CumFreeBytes)
    各段落仅在对应计数 > 0 时输出

  Phase 5 — 写入输出
    write(2, buf, n) 到 stderr
    若 GODEBUG=gcdeadtracefile=<path>，同时写入文件


7. 跨协程会话隔离原理

  场景：3 个 goroutine，只有 goroutine A 启动了会话

  ┌────────────┬──────────┬────────┬──────────────────────────────┐
  │  协程      │ 会话状态 │ 分配   │ bucket 的 gcDeadSession*     │
  ├────────────┼──────────┼────────┼──────────────────────────────┤
  │ Main       │ 无会话   │ 128B   │ AllocBytes=0 → 释放时不计数  │
  │ Goroutine A│ 有会话   │ 256B   │ AllocBytes>0 → 释放时会计数  │
  │ Goroutine B│ 无会话   │ 512B   │ AllocBytes=0 → 释放时不计数  │
  └────────────┴──────────┴────────┴──────────────────────────────┘

  隔离原理分三步：

  Step 1 — 分配时打标记
    Main 和 Goroutine B：gcDeadSessionActive == false
      → 不递增 gcDeadSessionAllocBytes
      → 对应 bucket 的 AllocBytes 保持 0

    Goroutine A：gcDeadSessionActive == true
      → 原子递增其分配到的 bucket 的 gcDeadSessionAllocBytes
      → 对应 bucket 的 AllocBytes > 0（"打过标记"）

  Step 2 — 释放时根据标记决策
    128B 对象释放（Main 分配的）：
      其 bucket 的 AllocBytes == 0 → 仅更新全局 gcDeadFrees
      不更新 gcDeadSessionFrees

    256B 对象释放（Goroutine A 分配的）：
      其 bucket 的 AllocBytes > 0 → 同步更新 gcDeadSessionFrees
      → 会话报告会包含此释放

    512B 对象释放（Goroutine B 分配的）：
      其 bucket 的 AllocBytes == 0 → 不更新 gcDeadSessionFrees

  Step 3 — GC 结束时输出（仅 session，无全局 gcdead:）
    gcdeadsession:freed: 仅包含 256B（打过标记的 bucket，本周期释放）
    gcdeadsession:alive: 0B（如果会话对象全部释放了）


8. 会话存活对象追踪 (v3)
========================

  问题：只知道本周期释放了多少会话对象，不知道还有多少会话对象"活着"。
  v3 解决方案：累计跟踪分配和释放，差 = 仍存活。

  累计字段设计：
    ┌────────────────────────┬──────────┬────────────────────────┐
    │ 字段                   │ 生命周期 │ 用途                   │
    ├────────────────────────┼──────────┼────────────────────────┤
    │ gcDeadSessionAllocs    │ 永不归零 │ 总会话分配次数         │
    │ gcDeadSessionAllocBytes│ 永不归零 │ 总会话分配字节         │
    │ gcDeadSessionFrees     │ 每周期清零│ 本周期会话释放次数     │
    │ gcDeadSessionFreeBytes │ 每周期清零│ 本周期会话释放字节     │
    │ gcDeadSessionCumFrees  │ 永不归零 │ 累计会话释放次数       │
    │ gcDeadSessionCumFreeBytes│永不归零│ 累计会话释放字节       │
    └────────────────────────┴──────────┴────────────────────────┘

  仍在分配：仅更新 gcDeadSessionAllocs / AllocBytes
  释放时：同时更新 per-cycle（Frees/FreeBytes）和 cumulative
          （CumFrees/CumFreeBytes）
  GC 结束：alive = Allocs - CumFrees, aliveBytes = AllocBytes - CumFreeBytes

  示例场景：
    ┌────────────────┬────────┬──────────┬──────────────────────┐
    │ 协程           │ 大小   │ 状态     │ 会话报告             │
    ├────────────────┼────────┼──────────┼──────────────────────┤
    │ Session Gor A  │ 256B   │ 已释放   │ gcdeadsession:freed  │
    │ Session Gor A  │ 1024B  │ 仍存活   │ gcdeadsession:alive  │
    │ Main (no sess) │ 128B   │ 已释放   │ gcdead 全局          │
    │ Gor B (no sess)│ 512B   │ 已释放   │ gcdead 全局          │
    └────────────────┴────────┴──────────┴──────────────────────┘


9. 关键文件

  src/runtime/runtime2.go:552  — g.gcDeadSessionActive 字段
  src/runtime/mprof.go:87      — memRecord: Allocs/CumFrees 等 6 个会话字段
  src/runtime/mprof.go:452     — mProf_Malloc 会话检查 (Allocs + AllocBytes)
  src/runtime/mprof.go:491     — mProf_Free 会话计数 (per-cycle + cumulative)
  src/runtime/mprof.go:557     — gcDeadTracePrint: Phase1 alive 计算 + Phase4 输出
  src/runtime/mprof.go:849     — GcDeadSessionStart
  src/runtime/mprof.go:864     — GcDeadSessionEnd
  src/runtime/mgc.go:1620      — GC 结束时调用 gcDeadTracePrint()
  src/runtime/runtime1.go:319  — debug.gcdeadtrace GODEBUG 注册
  src/runtime/create_file_unix.go    — Unix 文件写入 (O_APPEND)
  src/runtime/create_file_windows.go  — Windows 文件写入 (FILE_APPEND_DATA)
  src/runtime/create_file_nounix.go   — 其他平台 (no-op)


附录: GODEBUG 使用说明
======================

1. 基本用法

   // 启用 gcdeadtrace，输出到 stderr
   GODEBUG=gcdeadtrace=1 go run main.go

   // 同时输出到文件 (追加模式)
   GODEBUG=gcdeadtrace=1,gcdeadtracefile=gcdeadtrace.log go run main.go

   // 文件仅追加，不输出到 stderr
   GODEBUG=gcdeadtrace=1,gcdeadtracefile=gcdeadtrace.log go run main.go 2>/dev/null

2. 输出格式

   仅输出 session 数据，无全局 gcdead: 报告:

   gcdeadsession:freed: N session objs (M bytes) ...      ← 会话内分配且已释放
     funcName (file:line): N session objs, M session bytes
     funcName (file:line): N session objs, M bytes

   gcdeadsession:alive: N session objs (M bytes) ...      ← 会话内分配仍存活
     funcName (file:line): N session objs, M bytes

3. Session API

   runtime.GcDeadSessionStart()
   // ... 分配会被追踪为 session 分配 ...
   runtime.GcDeadSessionEnd()
   // ... 后续分配不在 session 内 ...

4. 文件输出说明

   - gcdeadtracefile=<path> 追加模式，多次 GC 周期/多次运行的结果不丢失
   - 文件不存在自动创建
   - 与 stderr 输出同时生效（两者互不影响）
   - 只输出 gcdeadtrace 内容，不包含普通程序日志
